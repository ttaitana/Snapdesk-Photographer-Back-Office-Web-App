/**
 * @snapdesk/core — domain/service layer.
 *
 * Empty on purpose for P0. Services (teams, customers, jobs, payments,
 * team-context) land starting P1/P2 per TASKS.md. Keep this package free of
 * any `apps/*` or Next.js import — see README.md.
 */

export const CORE_PACKAGE_NAME = "@snapdesk/core" as const;
