// DEPRECATED — superseded by P5 F8's SidebarNav/BottomNav
// (components/sidebar-nav.tsx, components/bottom-nav.tsx), which AppShell
// now renders instead of this flat, non-responsive nav. No file in the repo
// imports TeamNav anymore. Kept on disk only because this sandbox's mount
// doesn't allow deleting files (`rm`/`os.remove` both fail with "Operation
// not permitted") — safe to delete this file by hand.
export {};
